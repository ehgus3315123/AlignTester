<Window x:Class="TMTestWpfApp.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Log" Height="560" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="#F4F7FA">
    <Window.Resources>
        <Style x:Key="ErrorRow" TargetType="DataGridRow">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Level}" Value="Error">
                    <Setter Property="Foreground" Value="#C0392B"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Border Margin="12" Background="#FFFFFF" BorderBrush="#DCE4EB" BorderThickness="1" CornerRadius="6">
        <DataGrid x:Name="dgLog"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  BorderThickness="0"
                  Background="Transparent"
                  RowStyle="{StaticResource ErrorRow}"
                  FontFamily="Consolas" FontSize="13"
                  ColumnHeaderHeight="28"
                  RowHeight="26">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Level"   Binding="{Binding Level}"   Width="56"/>
                <DataGridTextColumn Header="Time"    Binding="{Binding TimeStr}" Width="110"/>
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Border>
</Window>
